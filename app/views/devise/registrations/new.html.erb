<div id="request-invite" class="modal" style="display: <%= @user.errors.any? ? 'block' : 'none';%>">
  <%= simple_form_for resource, :as => resource_name, :url => registration_path(resource_name) , :html => {:class => 'form-horizontal' } do |f| %>
    <div class="modal-header">
      <a class="close" data-dismiss="modal">&#215;</a>
      <h3>Request Invitation</h3>
    </div>
    <div class="modal-body">
      <%= f.error_notification %>
      <%= f.input :email, :placeholder => 'user@example.com' %>
    </div>
    <div class="modal-footer">
      <%= f.submit "Request Invitation", :class => "btn btn-inverse ", :id => 'invitation_button' %>
      <a class="btn" data-dismiss="modal" href="#">Close</a>
    </div>
  <% end %>
</div>
<div id="romance-copy"  style="text-align: center; margin-top: 80px">
  <h1><span class="bid">MojoBID</span></h1><br>
  <h2><span class="reg">You choose the dollar amount and time - and <span class="rest">restaurants</span> will <span class="bid">bid</span> for you!</h2></span>
  <br>
  <h3>There are <b><%=100-(User.count)%></b> invitations left for the greater Ft. Meyers area</he>
  <h2>Want in?</h2>
</div>
<div id="call-to-action" style="text-align: center; margin-top: 60px">
  <a class="btn btn-inverse btn-large" data-toggle="modal" href="#request-invite">Request invite</a>
</div>
